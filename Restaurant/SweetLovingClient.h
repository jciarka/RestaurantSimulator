#pragma once

#include "fwd_include.h"
#include "StandardClient.h"

/*
class SweetLovingClient // : public StandardClient
{
}

protected:

    // Functions to override in more specyfic clients
    //virtual void choose_dishes();

public:
    //SweetLovingClient(unsigned choosing_time, ITrigger* global_trigger, IRaporter* global_raporter);
    //virtual ~SweetLovingClient() override {};
}
*/
